# Base URL of the backend API (include /api).
VITE_API_BASE_URL=http://localhost:4000/api

# Public VAPID key for web-push subscription (must match backend VAPID_PUBLIC_KEY).
# Also available live from GET /api/settings once the Setting module exposes it.
VITE_VAPID_PUBLIC_KEY=

export function initCronJobs() {
  // Cron jobs disabled — all email reports are triggered explicitly via UI buttons as requested.
  console.log('✅ Cron scheduler initialized: Automatic background emails disabled.');
}
